# Browser-safe Supabase values, populated in Phase 3/5.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only values, add only when a later phase needs them.
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DATABASE_URL=

APP_ENV=development
MAX_UPLOAD_MB=10
